# Optional: if unset, the pipeline uses a rule-based stub classifier
# instead of calling a real LLM, so the project runs with zero API keys.
OPENAI_API_KEY=

# Optional: model name for the triage/investigation/decision agents
SENTINELOPS_MODEL=gpt-4o-mini
